apiVersion: v1
kind: Service
metadata:
  name: tracker
  labels:
    app: tracker
spec:
  clusterIP: None
  ports:
  - port: 5212
  selector:
    app: tracker
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: tracker
  labels:
    app: tracker
spec:
  serviceName: tracker
  replicas: 3
  selector:
    matchLabels:
      app: tracker
  template:
    metadata:
      labels:
        app: tracker
    spec:
      terminationGracePeriodSeconds: 35
      containers:
      - name: tracker
        image: webcrawler.tracker:0.2.1
        env:
        - name: CLUSTER_IP
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: CLUSTER_SEEDS
          value: akka.tcp://webcrawler@lighthouse-0.lighthouse:4053,akka.tcp://webcrawler@lighthouse-1.lighthouse:4053,akka.tcp://webcrawler@lighthouse-2.lighthouse:4053
        livenessProbe:
          tcpSocket:
            port: 5212
        ports:
        - containerPort: 5212
          protocol: TCP